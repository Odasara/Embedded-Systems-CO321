In the task 1 we only have to initialize two functions.

Task 2 code is divided to two parts named, task2 and task22, eeprom writing(task2) code should execute first and give an input using serial monitor.

Then task22 should be executed and the saved input could be read at serial monitor.

Task 3 is done using Tinkercad software.

***If doing in real hardware, use lcd.h instead of LiquidCrystal.h and change functions accordingly***
